# 딕셔너리 키 이름 중복

# 딕셔너리를 다음과 같이 만들었다. 
# 출력값을 입력하시오. (출력값은 공백을 넣지 않습니다.)

d = {'height':180,'weight':78,'weight':84,'temparture':36,'eyesight':1}
print(d['weight'])

# 답안
84

# 딕셔너리는 키 이름의 중복을 허용하지 않으며 중복되면 가장 마지막의 값이 출력된다.