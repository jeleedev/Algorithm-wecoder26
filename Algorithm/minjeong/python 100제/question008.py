# 문제8 : 딕셔너리 키 이름 중복

# 딕셔너리를 다음과 같이 만들었다. 
# 출력값을 입력하시오. (출력값은 공백을 넣지 않습니다. )


d = {'height':180,'weight':78,'weight':84,'temparture':36,'eyesight':1}
print(d['weight'])

# 답 84

# 딕셔너리에 키와 값을 저장할 때 키가 중복되면 가장 뒤에 있는 값만 사용,  중복되는 키는 저장되지 않는다.
