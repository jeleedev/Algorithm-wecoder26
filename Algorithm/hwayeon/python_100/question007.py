# 변수명

# 다음 중 변수명으로 사용할 수 없는 것 2개를 고르시오.
# 1) age
# 2) a
# 3) as
# 4) _age
# 5) 1age

# 답안
# 3), 5)

# as는 문법적인 용도로 사용하고 있는 예약어이므로 사용할 수 없으며
# 변수명은 숫자로 시작할 수 없다.

# 파이썬 변수명 생성 규칙

# 영문 문자와 숫자를 사용할 수 있다.
# 대소문자를 구분한다.
# 문자부터 시작해야 하며 숫자부터 시작하면 안된다.
# _(밑줄 문자)로 시작할 수 있다.
# 특수 문자(+, -, *, /, $, @, &, %등)는 사용할 수 없다.
# 파이썬의 키워드(if, for, while, and, or 등)는 사용할 수 없다.

# 예약어 예시

# and, elif, global, or, assert, else, if, pass, break, except, 
# import, print, class, exec, in, raise, continue, finally, is, 
# return, def, for, lambda, try, del, from, not, while, yield
